package com.lab.product.entity.ENUMS;

public enum PRODUCT_RULE_VALIDATION {
    MIN_MAX, // For ranges
    EXACT,   // For exact matches
    LIST     // For checking against a list of allowed values
}
